name: First Workflow
on: [push,workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: get the code
      uses: actions/checkout@v4
    - name: build the code
      run: php -l index.php
